[project]
name = "infertrace"
version = "0.1.0"
description = "Lightweight inference tracing SDK"
requires-python = ">=3.10"
dependencies = [
    "grpcio",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
